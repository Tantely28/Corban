{% extends 'base.html.twig' %}

{% block title %}Hello PosteController!{% endblock %}

{% block body %}
    <div class="container">
        <h1>Les temoignages du client : </h1>
        <table class="table table-striped">
            <a href="{{ path('ajout_temoin', {id:1}) }}" class="text-left">
                <div class="btn btn-primary">Ajouter témoignage</div>
            </a>
            <thead>
            <tr>
                <th>Titre</th>
                <th>Description</th>
            </tr>
            </thead>
            <tbody>
            {% for t in tem %}
                <tr>
                    <td>{{ t.title }}</td>
                    <td>{{ t.description }}</td>
                    <td>{{ t.video }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>


            <div class="row">
                <div class="col-md-3">
                    <div class="embed-responsive embed-responsive-16by9">

                    </div>
                </div>
            </div>
    </div>
{% endblock %}
